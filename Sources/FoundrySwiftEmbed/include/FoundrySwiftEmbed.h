// FoundrySwiftEmbed has no public C API; this header exists only because
// SwiftPM requires an `include/` directory for C targets. The exported
// `foundry_swift_embed_entry_point` symbol is consumed by Foundry via the
// FoundryExtension loader, not by any other SwiftPM target.

#ifndef FOUNDRY_SWIFT_EMBED_H
#define FOUNDRY_SWIFT_EMBED_H

#endif
