The current sample just removes all the other platforms from the list, and only
keeps the binary for Swift on MacOS.    It should bring back the format from the
foundry-cpp/test/demo directory eventually.
